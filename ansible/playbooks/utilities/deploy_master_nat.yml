- name: Copy NAT-based Lima configuration to macmini-01
  hosts: macmini-01
  become: no
  tasks:
    - name: Copy k3s-master-nat.yaml to macmini-01
      copy:
        src: ../k3s-master-nat.yaml
        dest: ~/k3s-master.yaml
