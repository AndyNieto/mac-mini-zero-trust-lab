- name: Install socket_vmnet
  hosts: mac_minis
  become: false
  tasks:
    - name: Install socket_vmnet Homebrew formula
      community.general.homebrew:
        name: socket_vmnet
        state: present
